name: i_am_a_hyena

description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - images/ # Es ist nicht nötig, jedes Bild einzeln aufzuzählen; es reicht, den Ordner
              # anzugeben, in dem sich die Bilder befinden, um auf alle zugreifen zu können.
